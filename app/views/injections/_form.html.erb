<%= form_for(@injection) do |f| %>
  <% if @injection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@injection.errors.count, "error") %> prohibited this injection from being saved:</h2>

      <ul>
      <% @injection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
